---
name: xiaohongshu-parser
description: 从小红书网页中提取和整理文章内容到本地文档系统。使用此技能当用户提供小红书链接、说"从小红书提取这篇文章"、"解析小红书内容"或需要处理小红书文章时。自动识别文字和图片内容，去除转发者评论，按日期归档保存为 Markdown 文档和元数据文件。
---

# 小红书文章解析

## 工作流程

### 1. 内容提取

使用 Chrome DevTools 工具获取页面内容：

- **文字内容**：使用 `take_snapshot` 获取页面文本
- **图片内容**：使用 `take_screenshot` 截取图片，然后用 `mcp__4_5v_mcp__analyze_image` 提取文字
- **多图片处理**：小红书文章通常包含多张图片，需逐个识别并拼接

**关键要点**：核心内容通常在图片中，不是 HTML 文本。

### 2. 内容整理

将提取的内容整理为干净的 Markdown 文档：

- **去除转发者**：删除"大家好，我是XX"、"关注我"等中间人评论
- **保留原创**：保留原作者的核心信息、技术要点、引用和比喻
- **结构化**：使用 Markdown 标题层级组织内容（# ## ###）
- **完整对照**：确保没有遗漏段落，特别是结尾的引用或比喻

### 3. 完整性验证

对照原始网页检查内容：

- 检查开头和结尾是否完整
- 验证引用、比喻是否遗漏
- 确认技术要点无缺失

### 4. 文件归档

按照项目 CLAUDE.md 中定义的结构创建文件：

```
articles/YYYY-MM-DD/article-slug.md      # 正文（可分享）
metadata/YYYY-MM-DD/article-slug.yaml    # 元数据（内部管理）
```

**重要**：
- 正文文件末尾保留原作者来源说明，不包含元数据引用
- 元数据的 `content_file` 使用相对路径指向正文（如 `../../articles/2025-01-11/...`）

## 资源

### references/

- **metadata-template.yaml**：元数据文件模板，包含所有必需字段
- **workflow-guide.md**：详细的分步工作指南

创建文件时参考这些模板确保结构一致性。
