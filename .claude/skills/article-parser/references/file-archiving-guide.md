# 文件归档指南

本文档定义通用的文章归档策略，适用于所有平台。

## 目录结构

```
general/article-slug/
├── article-slug.md              # 原始版本（./images/ 相对路径）
├── article-slug-standalone.md   # standalone 版本（base64 嵌入）
├── article-slug-remote.md       # remote 版本（CDN URL）
├── article-slug.metadata.yaml   # 元数据
└── images/                      # 本地图片目录
    ├── cover.jpg                # 描述性命名
    ├── workflow-diagram.png
    └── G_J8qXqaoAQ2xhu.jpg      # 或 Media ID 命名
```

## 三版本策略

**必须同时创建三个版本**！

| 版本 | 文件名 | 图片处理 | 适用场景 |
|------|--------|----------|----------|
| **原始版** | `article-slug.md` | `./images/` 相对路径 | 日常阅读、编辑、本地预览 |
| **standalone 版** | `article-slug-standalone.md` | base64 数据嵌入 | 离线分享、长期归档、邮件发送 |
| **remote 版** | `article-slug-remote.md` | 原始 CDN URL | 在线分享、博客发布、GitHub 预览 |

### 原始版本

```markdown
![图片说明](./images/cover.jpg)
```

- ✅ 优点：文件小、编辑方便、加载速度快
- ❌ 缺点：分享时对方看不到图片
- 🎯 适用：个人笔记、本地归档、日常编辑

### standalone 版本

```markdown
![图片说明](data:image/jpeg;base64,/9j/4AAQSkZJRg...)
```

- ✅ 优点：完全自包含，无需网络，任何人都能看到图片
- ❌ 缺点：文件较大
- 🎯 适用：离线分享、邮件附件、长期归档

### remote 版本

```markdown
![图片说明](https://mmbiz.qpic.cn/mmbiz_png/xxx.png)
```

- ✅ 优点：文件小、加载快、可在线预览
- ❌ 缺点：需要网络连接、依赖外部 CDN
- 🎯 适用：博客发布、在线文档、GitHub 预览

## 图片处理策略

### 统一下载策略

**所有平台的图片都下载到本地** `images/` 目录：

| 平台 | 原始 URL | 本地策略 |
|------|---------|----------|
| Twitter/X | `pbs.twimg.com/media/G_xxx` | 下载到 `images/G_xxx.jpg` |
| 微信公众号 | `mmbiz.qpic.cn/xxx` | 下载到 `images/xxx.jpg` |
| 知乎 | `zxpic.cn/xxx` | 下载到 `images/xxx.jpg` |
| 小红书 | `sns-webpic-qc.xhscdn.com/xxx` | 下载到 `images/xxx.jpg` |

**优点**：
- ✅ 统一的工作流程，无需判断平台
- ✅ 避免外链失效风险
- ✅ 便于生成 standalone 版本
- ✅ 本地预览不依赖网络

### 图片命名规范

| 策略 | 示例 | 适用场景 |
|------|------|----------|
| **描述性命名** | `workflow-diagram.png` | 推荐：语义清晰 |
| **Media ID** | `G_J8qXqaoAQ2xhu.jpg` | Twitter（自动提取） |
| **URL Hash** | `a1b2c3d4e5f6.jpg` | 其他平台（自动生成） |

**命名原则**：
- ✅ 使用描述性名称：`cover.jpg`、`workflow-diagram.png`
- ✅ 或使用 Media ID：`G_J8qXqaoAQ2xhu.jpg`
- ❌ 不要用纯数字索引：`image-01.jpg`、`img-1.png`
- ✅ 全部小写，空格用连字符替换

**推荐的命名方式**：

| 场景 | 推荐名称 | 不推荐 |
|------|---------|--------|
| 封面图 | `cover.jpg` | `img-1.jpg` |
| 工作流程图 | `workflow-diagram.png` | `image_2.png` |
| 代码截图 | `code-example.png` | `img-3.png` |
| 架构图 | `architecture.png` | `image_4.png` |

## 生成三版本的工作流程

### 步骤 1：下载所有图片

使用脚本或手动下载所有图片到 `images/` 目录。

### 步骤 2：生成原始版本

创建 `article-slug.md`，使用相对路径引用图片：

```markdown
# 文章标题

> 来源：平台名称 作者，YYYY-MM-DD
> 原文链接：https://...

![封面图](./images/cover.jpg)

正文内容...

![流程图](./images/workflow-diagram.png)
```

### 步骤 3：生成 standalone 和 remote 版本

使用提供的脚本自动生成：

```bash
# 生成 standalone 版本（base64 嵌入）
python scripts/generate_standalone.py article-slug/article-slug.md

# 生成 remote 版本（CDN URL）
python scripts/generate_remote.py article-slug/article-slug.md
```

脚本会自动：
1. 读取原始版本中的 `./images/xxx.jpg` 引用
2. **standalone**：将图片转换为 base64，替换为 `data:image/jpeg;base64,...`
3. **remote**：替换为原始 CDN URL

## 验证清单

生成三版本后，必须验证：

- [ ] 三个 `.md` 文件都存在（原始、standalone、remote）
- [ ] 原始版本使用 `./images/` 路径
- [ ] standalone 版本使用 `data:image/` 格式
- [ ] remote 版本使用原始 CDN URL
- [ ] `images/` 目录中的图片数量与原始版本引用数量一致
- [ ] 图片命名符合规范（描述性或 Media ID）

## Git 提交策略

**重要**：提交时包含所有三个版本和图片目录。

```bash
git add general/article-slug/
git commit -m "$(cat <<'EOF'
Add: 文章标题

包含原始版本、standalone 版本、remote 版本及元数据。

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
EOF
)"
```

**注意**：
- ✅ 提交前确保三个版本都已生成
- ✅ 确保 `images/` 目录中的图片完整
- ✅ 确保元数据文件完整
